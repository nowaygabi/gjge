<configuration>
    <property name="LOG_FILE" value="logs/gjge.log"/>
    <property name="MAX_LOG_FILE_SIZE" value="10MB"/>
    <property name="TOTAL_LOG_FILE_SIZE" value="100MB"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%logger{36}] - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i</fileNamePattern>
            <maxFileSize>${MAX_LOG_FILE_SIZE}</maxFileSize>
            <totalSizeCap>${TOTAL_LOG_FILE_SIZE}</totalSizeCap>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level[%thread] [%logger{36}] - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <logger name="com.nowaygabi.engine.Window" level="info"/>
    <logger name="com.nowaygabi.engine.rendering" level="debug"/>
    <logger name="com.nowaygabi.engine.physics" level="debug"/>
    <logger name="com.nowaygabi.engine.ai" level="warn"/>
</configuration>
